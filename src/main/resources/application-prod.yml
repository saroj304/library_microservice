spring:
  config:
    activate:
      on-profile: prod


  datasource:
    url: jdbc:postgresql://172.17.0.1:5433/library_microservice
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

